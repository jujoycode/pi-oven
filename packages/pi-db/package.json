{
  "name": "pi-db",
  "version": "0.1.0",
  "description": "Query MySQL and Oracle from Pi: read-only SQL against named connections in .pi/db.json, results as markdown tables. Credentials never enter the conversation.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "dependencies": {
    "mysql2": "^3.14.0",
    "oracledb": "^6.8.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.6",
    "typebox": "^1.3.6"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
